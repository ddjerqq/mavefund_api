import Cell from 'gridjs/dist/src/cell';
import Base from 'gridjs/dist/src/base';
import { TCell } from 'gridjs/dist/src/types';
declare class Row extends Base {
    private _cells;
    constructor(cells?: Cell[]);
    cell(index: number): Cell;
    get cells(): Cell[];
    set cells(cells: Cell[]);
    toArray(): TCell[];
    static fromCells(cells: Cell[]): Row;
    get length(): number;
}
export default Row;
