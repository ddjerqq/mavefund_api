import { Config } from 'gridjs/dist/src/config';
import { VNode } from 'preact';
import { EventEmitter } from 'gridjs/dist/src/util/eventEmitter';
import { GridEvents } from 'gridjs/dist/src/events';
import { PluginManager } from 'gridjs/dist/src/plugin';
declare class Grid extends EventEmitter<GridEvents> {
    config: Config;
    plugin: PluginManager;
    constructor(config?: Partial<Config>);
    updateConfig(config: Partial<Config>): this;
    createElement(): VNode;
    forceRender(): this;
    destroy(): void;
    render(container: Element): this;
}
export default Grid;
